name: Daily Select
# Updated on 2026-01-31 - Git conflict 対応追加

on:
  workflow_dispatch:

  schedule:
    - cron: '30 0 * * *'  # 毎日 09:30 JST（UTC 0:30）

jobs:
  daily-select:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install requests beautifulsoup4
      
      - name: Get date
        id: get_ymd
        run: |
          ymd=$(TZ=Asia/Tokyo date +%Y%m%d)
          echo "ymd=${ymd}" >> $GITHUB_OUTPUT
          echo "📅 対象日付: ${ymd}"
      
      - name: Run daily select
        run: |
          python scripts/daily_select.py ${{ steps.get_ymd.outputs.ymd }}
      
      - name: Fetch shutuba data
        run: |
          python scripts/fetch_shutuba.py ${{ steps.get_ymd.outputs.ymd }}
      
      - name: Fetch past races data
        run: |
          python scripts/fetch_past_races.py ${{ steps.get_ymd.outputs.ymd }}
      
      - name: Calculate DES scores
        run: |
          python scripts/calculate_des_score.py ${{ steps.get_ymd.outputs.ymd }}
      
      - name: Select predictions
        run: |
          python scripts/select_predictions.py ${{ steps.get_ymd.outputs.ymd }}
      
      # Generate final output（エラーでも続行）
      - name: Generate final output
        continue-on-error: true
        run: |
          python scripts/generate_final_output.py ${{ steps.get_ymd.outputs.ymd }}
      
      # ★ 最新予想ファイルをコピー
      - name: Copy latest predictions
        run: |
          echo "📋 最新予想ファイルをコピー中..."
          
          # final_predictions_*.json を latest_predictions.json にコピー
          if ls final_predictions_*.json 1> /dev/null 2>&1; then
            latest_file=$(ls -t final_predictions_*.json | head -n 1)
            cp "$latest_file" latest_predictions.json
            echo "✅ $latest_file を latest_predictions.json にコピーしました"
            ls -lh latest_predictions.json
          else
            echo "⚠️ final_predictions_*.json が見つかりません"
          fi
          
          # predictions_*.md を latest_predictions.md にコピー
          if ls predictions_*.md 1> /dev/null 2>&1; then
            latest_md=$(ls -t predictions_*.md | head -n 1)
            cp "$latest_md" latest_predictions.md
            echo "✅ $latest_md を latest_predictions.md にコピーしました"
            ls -lh latest_predictions.md
          else
            echo "⚠️ predictions_*.md が見つかりません"
          fi
          
          # race_data_*.json を latest_race_data.json にコピー
          if ls race_data_*.json 1> /dev/null 2>&1; then
            latest_race=$(ls -t race_data_*.json | head -n 1)
            cp "$latest_race" latest_race_data.json
            echo "✅ $latest_race を latest_race_data.json にコピーしました"
            ls -lh latest_race_data.json
          fi
      
      # ★ 修正: Git pull を追加してコンフリクトを回避
      - name: Commit race_data JSON
        run: |
          echo "📂 現在のファイル一覧:"
          ls -lh race_data_*.json 2>/dev/null || echo "race_data_*.json が見つかりません"
          ls -lh today_jobs.latest.json 2>/dev/null || echo "today_jobs.latest.json が見つかりません"
          ls -lh latest_predictions.json 2>/dev/null || echo "latest_predictions.json が見つかりません"
          ls -lh latest_predictions.md 2>/dev/null || echo "latest_predictions.md が見つかりません"
          ls -lh latest_race_data.json 2>/dev/null || echo "latest_race_data.json が見つかりません"
          
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # ★ リモートの変更を取得（コンフリクト回避）
          echo "🔄 リモートの変更を取得中..."
          git fetch origin main
          git pull --rebase origin main || {
            echo "⚠️ Pull failed, trying merge strategy..."
            git pull --no-rebase origin main
          }
          
          # 各ファイルパターンを個別に追加（存在しないものはスキップ）
          git add race_data_*.json 2>/dev/null || true
          git add today_jobs.latest.json 2>/dev/null || true
          git add predictions_*.md 2>/dev/null || true
          git add final_predictions_*.json 2>/dev/null || true
          git add warnings_report_*.md 2>/dev/null || true
          git add latest_predictions.json 2>/dev/null || true
          git add latest_predictions.md 2>/dev/null || true
          git add latest_race_data.json 2>/dev/null || true
          
          echo "📊 ステージングされたファイル:"
          git diff --cached --name-only
          
          if ! git diff --cached --quiet; then
            echo "💾 コミット実行中..."
            git commit -m "chore: update race data with predictions for ${{ steps.get_ymd.outputs.ymd }}"
            
            # ★ Push 前にもう一度 pull（他のワークフローとの競合回避）
            git pull --rebase origin main || true
            
            git push
            echo "✅ コミット完了"
          else
            echo "⚠️ 変更なし（コミット不要）"
          fi

      # Validate predictions はエラーでも続行
      - name: Validate predictions
        continue-on-error: true
        run: |
          python scripts/validate_predictions.py ${{ steps.get_ymd.outputs.ymd }}
